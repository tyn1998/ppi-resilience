# 很大的数据，当然要忽略
data/input/ppi_graph_statistics

# clang-format配置文件（格式化代码）
.clang-format

# clangd配置文件（为了指定头文件路径，但是directory必须是绝对路径因此不通用）
compile_commands.json

# clangd缓存目录
.cache

# 生成的头文件依赖
.depend

bin
obj
